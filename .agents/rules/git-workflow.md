# Git & Deployment Workflow

- After completing requested modifications or features in this project, automatically stage, commit with a descriptive message, and push the changes to GitHub (`origin main`).
- This ensures Vercel automatically deploys every change immediately.
